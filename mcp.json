{
  "mcpServers": {
    "cloudtalk-api": {
      "command": "pnpm",
      "args": [
        "--dir",
        "mcp/cloudtalk-api",
        "exec",
        "node",
        "dist/index.js"
      ],
      "env": {
        "BASE_URL": "http://localhost:3000"
      }
    },
    "cloudtalk-db": {
      "command": "pnpm",
      "args": [
        "--dir",
        "mcp/cloudtalk-db",
        "exec",
        "node",
        "dist/index.js"
      ],
      "env": {
        "DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/reviews_dev"
      }
    }
  }
}
